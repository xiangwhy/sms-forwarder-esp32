# v4.0.23 — 信 DCS 严格走, 错位/reserved/无效编码 → 丢 body Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 main.cpp decode 路径从 v4.0.22 的"sniff + is_strict_utf8 fallback UCS-2"激进策略改成 AOSP/smstools3/python-messaging 一致的"严格信 DCS, 错位/reserved → 丢 body"行为, 真机复现 dtac 乱码 SMS 应见 body="".

**Architecture:** 单 file `src/main.cpp:1820-1980` decode 路径 3 处删 + 4 丢 body 触发条件加. `src/pdu_codec.cpp` 0 改 (sniff helper `looks_like_ucs2_be` / `is_strict_utf8` / `pdu_ud_offset_ex` 留供未来重启用, 仅 main.cpp 不调). host test 加 fixture D/E/F + fixture A 加 bodyN=0 断言. bump FW_VERSION 2 处.

**Tech Stack:** C++17, ESP-IDF, ESP32-S3, ML307 4G, host test (CMake + GoogleTest-style 自写 CHECK macro), PlatformIO.

---

## File Structure

| File | Responsibility | Modify? |
|---|---|---|
| `src/main.cpp` | decode 路径 (line 1820-1980) — 删 sniff + 加 4 丢 body 触发 + FW_VERSION 宏 bump | ✓ |
| `src/pdu_codec.cpp` | sniff helper (looks_like_ucs2_be / is_strict_utf8 / pdu_ud_offset_ex) | ✗ (0 改) |
| `tests/host/test_pdu_codec.cpp` | host test — 加 fixture D/E/F + fixture A 加 bodyN=0 断言 | ✓ |
| `src/web/app.h` | 主页可见 fw-tag (line 42) | ✓ (bump 2 处之一) |

辅助: `src/main.cpp:1611-1631` `fixture_sniffer_decode` 测试 helper 镜像 main.cpp decode 算法. v4.0.23 加 force_drop / strict-utf8 fail / UDL<UDH 三个 flag 后, fixture D/E/F 通过测试这些 flag 触发 body="" 验证. 注意: host test **仅 main.cpp mirror 函数**测 (按 [[feedback_test_on_production_code]] 测试绑生产代码 .cpp 跑), 不动 pdu_codec fixture 主体.

---

## Task 1: TDD red fixture D — DCS=0xFF reserved → body=""

**Files:**
- Modify: `tests/host/test_pdu_codec.cpp` (在 fixture A 之前插入 `test_pdu_e2e_dtac_dcs255_drop_body()`)
- Modify: `tests/host/test_pdu_codec.cpp` `int main()` (在 fixture A 调用前注册 D)

- [ ] **Step 1: 写 fixture D 函数 (在 fixture A 之前插入)**

```cpp
// Fixture D: v4.0.23 TDD red — DCS=0xFF reserved 触发 force_drop → body=""
//   翔哥 2026-06-22 真机复现 dtac +66813079348 boot #128 乱码 case (raw bytes 全 0x00/0xXX 异常)
//   +CMT HEAD dcs=0xFF reserved (3GPP TS 23.038 §4 reserved), TPDU DCS byte=0x00 (7-bit, 标错)
//   v4.0.23: pdu_ud_offset_ex 读 TPDU DCS=0x00 → is7bit=true → 7-bit decode → 不 strict UTF-8 → 丢 body
//   v4.0.22 red: sniff bypass → 强 UCS-2 decode → 出乱码 (ÅV8HdAøì...) → 期望 fail
static void test_pdu_e2e_dtac_dcs255_drop_body() {
  g_current = "E2E dtac DCS=0xFF reserved (real DCS=0) → body=\"\" (v4.0.23 strict-DCS drop)";
  // 翔哥 boot #128 收到 1 条 dtac SMS, body 全 0x00/0xXX 异常字节 (Å=0xC5, V=0x56, 8=0x38, H=0x48, d=0x64...)
  // 这是 [[dtac-gateway-anomaly]] 6/20 同源 case. 简化: SCA(8)+FO(1)+oaLen(1)+ToA(1)+oaAddr(4)+PID(1)+DCS(1)+SCTS(7)+UDL(1)+UD(?)=26 bytes=52 hex
  const char* body =
    "07916649520080F0"  // SCA (8 bytes)
    "40"                  // FO UDHI=0 (1 byte)
    "07"                  // oaLen=7
    "91"                  // ToA=international BCD
    "8130793F"            // oaAddr (4 bytes)
    "00"                  // PID
    "FF"                  // DCS=0xFF reserved (TPDU 真相)
    "32153382890608"      // SCTS (7 bytes)
    "14"                  // UDL=20
    "C50038560048006400"  // UD raw bytes 像 UCS-2 BE 异常 (ÅV8Hd)
    "4100F800EC002100"    // UD raw bytes 续 (Aøì!)
    "4300C60046003800";   // UD raw bytes 续 (CF8)
  size_t bodyLen = std::strlen(body);

  bool isUcs2 = false, is7bit = false;
  char utf8[256] = {0};
  size_t n = fixture_sniffer_decode(body, bodyLen, &isUcs2, &is7bit, utf8, sizeof(utf8) - 1);
  utf8[n] = 0;

  // v4.0.23 strict-DCS: TPDU DCS=0xFF reserved → 7-bit decode (因 pdu_ud_offset_ex 标 is7bit=true) →
  //   不 strict UTF-8 (raw bytes 高 septet) → 丢 body (bodyN=0)
  // v4.0.22 red: 走 UCS-2 fallback (sniff bypass) → 乱码 (ÅV8HdAøì...) → n > 0 FAIL
  CHECK(n == 0);  // v4.0.23 PASS, v4.0.22 FAIL (red)
}
```

- [ ] **Step 2: 在 int main() 注册 fixture D (在 fixture A 之前)**

在 `int main()` 第 1906 行 `test_pdu_e2e_dtac_3seg_otp_5481_real_pdu();` 之前插入:

```cpp
  // v4.0.23 TDD red: 翔哥 6/22 boot #128 抓的 1 条 dtac DCS=0xFF 异常 SMS → body="" (strict-DCS drop)
  test_pdu_e2e_dtac_dcs255_drop_body();
```

- [ ] **Step 3: 增量编译 + 跑 host test, 期望 fixture D FAIL (red)**

Run:
```bash
cd /Users/xiang/Claude/Projects/esp32-sms/tests/host && cmake --build build 2>&1 | tail -20
```

Expected: 编译成功 (新加 fixture D 编译过).

Run:
```bash
./build/test_pdu_codec 2>&1 | grep -A2 "dtac DCS=0xFF"
```

Expected: 输出 `[FAIL]` 行 (current v4.0.22 走 UCS-2 fallback, n > 0, 不为 0, CHECK 失败).

- [ ] **Step 4: Commit TDD red D**

```bash
cd /Users/xiang/Claude/Projects/esp32-sms && git add tests/host/test_pdu_codec.cpp && git -c user.email=xiang@anthropic.com -c user.name=xiang commit -m "test(host): TDD red fixture D dtac DCS=0xFF reserved → body=\"\"

v4.0.23 strict-DCS spec (50bda13) 4 丢 body 触发条件 case 1: DCS 0x80-0xBF reserved.
翔哥 2026-06-22 boot #128 dtac 真乱码: TPDU DCS=0x00, +CMT HEAD dcs=0xFF (ML307 标错).
raw UD bytes 全 0x00/0xXX 异常 (DTAC gateway 推错编码, [[dtac-gateway-anomaly]] 同源).

v4.0.23 期望: body=\"\" (n=0, 丢 body).
v4.0.22 实际: sniff bypass → UCS-2 decode → 乱码 n>0, CHECK 失败 (TDD red 命中).

Mirror main.cpp:1847-1980 decode 路径, 不动 pdu_codec.cpp (sniff helper 留)."
```

---

## Task 2: TDD red fixture E — 7-bit decode 不 strict UTF-8 → body=""

**Files:**
- Modify: `tests/host/test_pdu_codec.cpp` (在 fixture D 之前/之后插入 `test_pdu_e2e_7bit_drop_body_not_strict_utf8()`)
- Modify: `tests/host/test_pdu_codec.cpp` `int main()` (注册 E)

- [ ] **Step 1: 写 fixture E 函数**

```cpp
// Fixture E: v4.0.23 TDD red — 7-bit decode 输出不 strict UTF-8 → 丢 body
//   DCS=0 标 7-bit, 但 raw bytes 高 septet 异常 (sub-byte drift / 错位)
//   v4.0.23: 7-bit decode 后 is_strict_utf8 fail → 丢 body (不再 fallback UCS-2)
//   v4.0.22 red: is_strict_utf8 fail → fallback UCS-2 decode → 乱码 n>0 FAIL
static void test_pdu_e2e_7bit_drop_body_not_strict_utf8() {
  g_current = "E2E 7-bit decode not strict UTF-8 → body=\"\" (v4.0.23 strict-DCS drop)";
  // DCS=0 7-bit, raw UD bytes 高 septet 异常 (DTAC gateway 推错, [[dtac-gateway-anomaly]])
  //   9 bytes PDU UD: 0xFF 0x10 0xFE 0x32 0x00 0x48 0x10 0xC5 0x56
  //   7-bit decode: septetCount=10, numChars=cmt_length=10
  //   packed decode → 含 lone continuation / overlong sequence → 不 strict UTF-8
  const char* body =
    "07916649520080F0"  // SCA (8 bytes)
    "40"                  // FO UDHI=0
    "07"                  // oaLen=7
    "91"                  // ToA=BCD
    "8130793F"            // oaAddr (4 bytes)
    "00"                  // PID
    "00"                  // DCS=0 (7-bit default, TPDU 真相)
    "32153382890608"      // SCTS (7 bytes)
    "0A"                  // UDL=10 septets
    "FF10FE32004810C556"; // UD 9 bytes raw (高 septet 异常)
  size_t bodyLen = std::strlen(body);

  bool isUcs2 = false, is7bit = false;
  char utf8[256] = {0};
  size_t n = fixture_sniffer_decode(body, bodyLen, &isUcs2, &is7bit, utf8, sizeof(utf8) - 1);
  utf8[n] = 0;

  // v4.0.23 strict-DCS: 7-bit decode 输出不 strict UTF-8 → 丢 body (bodyN=0)
  // v4.0.22 red: fallback UCS-2 decode → 输出 9 字节 raw 当 UCS-2 字符 → n>0 FAIL
  CHECK(n == 0);
}
```

- [ ] **Step 2: 在 int main() 注册 fixture E (在 fixture D 后)**

```cpp
  // v4.0.23 TDD red: 7-bit decode 不 strict UTF-8 → body="" (case 3)
  test_pdu_e2e_7bit_drop_body_not_strict_utf8();
```

- [ ] **Step 3: 增量编译 + 跑 host test, 期望 fixture E FAIL (red)**

Run:
```bash
cd /Users/xiang/Claude/Projects/esp32-sms/tests/host && cmake --build build 2>&1 | tail -10 && ./build/test_pdu_codec 2>&1 | grep -A2 "7-bit decode not strict"
```

Expected: 输出 `[FAIL]` 行.

- [ ] **Step 4: Commit TDD red E**

```bash
cd /Users/xiang/Claude/Projects/esp32-sms && git add tests/host/test_pdu_codec.cpp && git -c user.email=xiang@anthropic.com -c user.name=xiang commit -m "test(host): TDD red fixture E 7-bit decode not strict UTF-8 → body=\"\"

v4.0.23 strict-DCS spec (50bda13) case 3: 7-bit decode 输出不 strict UTF-8 → 丢 body.
v4.0.22 走 is_strict_utf8 fallback UCS-2 (sniff 激进) → 乱码 n>0, CHECK 失败 (TDD red 命中).
v4.0.23 期望: body=\"\" (n=0).

DCS=0 标 7-bit, raw UD 高 septet 异常 (DTAC gateway 推错编码, [[dtac-gateway-anomaly]])."
```

---

## Task 3: TDD red fixture F — UDL < UDH size → body=""

**Files:**
- Modify: `tests/host/test_pdu_codec.cpp` (插入 `test_pdu_e2e_udl_lt_udh_drop_body()`)
- Modify: `tests/host/test_pdu_codec.cpp` `int main()` (注册 F)

- [ ] **Step 1: 写 fixture F 函数**

```cpp
// Fixture F: v4.0.23 TDD red — UDL < UDH size → 丢 body (AOSP bufferLen<0 silent truncation 一致)
//   DCS=0, FO UDHI=1, UDH 7 bytes (16-bit concat), UDL=5 (声明比 UDH 还小)
//   v4.0.23: bufferLen<0 → 丢 body (AOSP 行为)
//   v4.0.22 red: bufferLen=0 → decode 空 UD (0 bytes) → n=0 不 fail 但 v4.0.23 strict 显式 drop
static void test_pdu_e2e_udl_lt_udh_drop_body() {
  g_current = "E2E UDL<UDH size → body=\"\" (v4.0.23 strict-DCS drop, AOSP bufferLen<0)";
  // DCS=0 7-bit, FO UDHI=1, UDH 7 bytes, UDL=5 (声明比 UDH 还小, ML307 quirk)
  const char* body =
    "07916649520080F0"  // SCA (8 bytes)
    "40"                  // FO UDHI=1
    "07"                  // oaLen=7
    "91"                  // ToA=BCD
    "8130793F"            // oaAddr (4 bytes)
    "00"                  // PID
    "00"                  // DCS=0 7-bit
    "32153382890608"      // SCTS (7 bytes)
    "05"                  // UDL=5 (< UDH size 7, 触发 bufferLen<0)
    "050003BCB40301"      // UDH 7 bytes 16-bit concat
    "C50038564800";       // UD 5 bytes (raw, 但 bufferLen<0 应全丢)
  size_t bodyLen = std::strlen(body);

  bool isUcs2 = false, is7bit = false;
  char utf8[256] = {0};
  size_t n = fixture_sniffer_decode(body, bodyLen, &isUcs2, &is7bit, utf8, sizeof(utf8) - 1);
  utf8[n] = 0;

  // v4.0.23 strict-DCS: bufferLen<0 → 丢 body (AOSP 行为, 显式 drop 标记)
  // v4.0.22 red: 走 decode_body_field 即使 bufferLen<0 也尝试 → 输出可能非空 n>0 或 raw bytes, FAIL
  CHECK(n == 0);
}
```

- [ ] **Step 2: 在 int main() 注册 fixture F**

```cpp
  // v4.0.23 TDD red: UDL<UDH size → body="" (case 4, AOSP bufferLen<0)
  test_pdu_e2e_udl_lt_udh_drop_body();
```

- [ ] **Step 3: 增量编译 + 跑 host test, 期望 fixture F FAIL (red)**

Run:
```bash
cd /Users/xiang/Claude/Projects/esp32-sms/tests/host && cmake --build build 2>&1 | tail -10 && ./build/test_pdu_codec 2>&1 | grep -A2 "UDL<UDH"
```

Expected: 输出 `[FAIL]` 行 (current v4.0.22 不显式 drop UDL<UDH case).

- [ ] **Step 4: Commit TDD red F**

```bash
cd /Users/xiang/Claude/Projects/esp32-sms && git add tests/host/test_pdu_codec.cpp && git -c user.email=xiang@anthropic.com -c user.name=xiang commit -m "test(host): TDD red fixture F UDL<UDH size → body=\"\"

v4.0.23 strict-DCS spec (50bda13) case 4: UDL<UDH size (AOSP bufferLen<0 silent truncation).
v4.0.22 不显式 drop, bufferLen<0 仍尝试 decode (输出非空或 raw bytes), FAIL (TDD red 命中).
v4.0.23 期望: body=\"\" (n=0, 显式 drop 标记).

ML307 quirk: UDH 头 7 字节但 UDL 标 5, 真实场景 concat partial 丢段或 gateway 推错 UDL."
```

---

## Task 4: 翔哥 审 TDD red 3 commit (D/E/F)

- [ ] **Step 1: 等翔哥 审 3 commit diff**

Show:
```bash
cd /Users/xiang/Claude/Projects/esp32-sms && git log --oneline -3
```

翔哥 审 3 commit: TDD red fixture D/E/F 跑挂 v4.0.22, 期待 v4.0.23 fix 后 PASS.

翔哥 拍板后进入 Task 5.

---

## Task 5: ~~Fixture A 期望调整 — 加 bodyN=0 断言~~ (翔哥 2026-06-22 撤回, 见 commit e4eb4b6 + 撤回)

**翔哥 2026-06-22 拍板撤回**: fixture A 保持 v4.0.22 期望 (SCA leak + n > 30). 撤回 spec 50bda13 "改 2 断言" 决策. C2 改后 fixture A 变 n==0 FAIL 是 case 3 触发的预期, 翔哥 接受留 **v4.0.24+** 修. 跟 [[feedback_working_code_no_touch]] trade 一致: 真机乱码复现要修 (strict-DCS), 顺手破坏 fixture A 接受, 不预先扩 scope.

**当前 (2026-06-22)**: fixture A 仍 v4.0.22 期望 (SCA leak + n > 30). 撤回前 commit 726124d 已 `git reset --hard HEAD~1` 撤回. HEAD = e4eb4b6 (注释 fix).

**Files:**
- ~~Modify: `tests/host/test_pdu_codec.cpp` (改 fixture A 期望)~~ — **不动**

- [ ] ~~Step 1: 改 fixture A 期望~~ — **跳过**
- [ ] ~~Step 2: 跑 fixture A 期望 FAIL (red)~~ — **跳过**
- [ ] ~~Step 3: Commit fixture A 调整~~ — **跳过 (commit 726124d 撤回)**

---

## Task 6: 翔哥 审 fixture A adjust

- [ ] **Step 1: 等翔哥 审 commit diff**

Show:
```bash
cd /Users/xiang/Claude/Projects/esp32-sms && git log --oneline -1
```

翔哥 拍板后进入 Task 7 (C2).

---

## Task 7: C2 — main.cpp decode 路径改 (核心)

**Files:**
- Modify: `src/main.cpp:1874-1896` (删兜底 sniff 3 分支)
- Modify: `src/main.cpp:1897-1905` (删 is7bit + looks_like_ucs2_be bypass)
- Modify: `src/main.cpp:1925-1932` (删 is_strict_utf8 fallback UCS-2, 改 drop body)
- Modify: `src/main.cpp:~1820-1980` (加 4 丢 body 触发 + ESP_LOGW)
- Modify: `src/main.cpp:1611-1631` `fixture_sniffer_decode` 测试 helper (镜像 main.cpp 改)

- [ ] **Step 1: 删 `src/main.cpp:1874-1896` 兜底 sniff 3 分支**

Replace (line 1874-1896):
```cpp
      // v4.0.11 fix: concat/partial path (udHexOff=0) 强制走 fallback 启发式
      //   真单条 SMS + reserved DCS (0x0C-0x0F / 0xFF 等) 也走这里 sniff
      if (!is7bit && !isUcs2) {
        // 兜底: 既不是 7-bit 也不是 UCS-2 → sniff
        if (pdu::looks_like_ucs2_be(udHex, udFullHexLen)) {
          isUcs2 = true;
          is8bitData = false;
        } else if (msg.cmt_length > 0 && udBytes > 0) {
          // 7-bit: udBytes ≈ cmt_len*7/8 ±2 (concat/partial path 也走这条 — numChars 已 sum)
          size_t expect = (msg.cmt_length * 7 + 7) / 8;
          if (udBytes >= expect - 2 && udBytes <= expect + 2) {
            is7bit = true;
            is8bitData = false;
          }
        } else if (udBytes > 0 && udBytes < 300) {
          // v4.0.11.1 fix: concat path cmt_len=0 (ML307 len=0 quirk) 时无 cmt_length 可信
          //   经验: TRUE/DTAC/Verify/AIS/KBank 拼接短信 99% 是 7-bit (≤200 octets)
          //   真 concat UCS-2 短信极罕见 (泰国运营商走 7-bit 优先)
          //   → 短 UD (10..200 octets) 且不像 UCS-2 → 默认 7-bit
          //   numChars ≈ udBytes * 8 / 7 (7-bit packed 密度)
          is7bit = true;
          is8bitData = false;
        }
        // else: 太长或 cmt_length 不可信, 默认 8-bit raw
      }
```

With (v4.0.23 strict-DCS drop):
```cpp
      // v4.0.23 strict-DCS (跟 AOSP / smstools3 / python-messaging 一致):
      //   pdu_ud_offset_ex 已读 TPDU DCS, 严格信. DCS 不在 7-bit/UCS-2/8-bit 三类
      //   (DCS 0x80-0xBF / 0xFC-0xFF reserved, [[dtac-gateway-anomaly]] 异常 bytes) → 丢 body.
      //   AOSP `parseUserData()` 走 `ENCODING_UNKNOWN` → `mMessageBody = null`, 我们 force_drop.
      if (!is7bit && !isUcs2) {
        // 既不是 7-bit 也不是 UCS-2 → DCS reserved / ML307 标错 / 网关推错编码 → 丢 body
        ESP_LOGW(TAG, "SMS: DCS reserved/unknown (dcs=%u cmt_len=%u udBytes=%u), drop body",
                 msg.dcs, msg.cmt_length, (unsigned)udBytes);
        force_drop = true;  // 触发下方丢 body
        is8bitData = false; // 防止下面 8-bit raw decode 跑
      }
```

- [ ] **Step 2: 删 `src/main.cpp:1897-1905` is7bit + looks_like_ucs2_be bypass**

Replace (line 1897-1905):
```cpp
      // 兜底 2: 即使 DCS 标 7-bit (含 DCS=0), raw body 若呈 UCS-2 BE 模式 → 强制走 UCS-2
      //   is_strict_utf8 兜底不靠谱 (GSM7 扩展字符 è/ø/Å/ò 都是合法 2-byte UTF-8)
      //   提前 sniff 避免跑 7-bit decode 浪费 + 防乱码推送
      if (is7bit && pdu::looks_like_ucs2_be(udHex, udFullHexLen)) {
        ESP_LOGW(TAG, "SMS: raw body UCS-2 BE pattern, bypass 7-bit decode (dcs=%u cmt_len=%u udBytes=%u)",
                 msg.dcs, msg.cmt_length, (unsigned)udBytes);
        is7bit = false;
        is8bitData = false;  // 强制走 else 分支 (decode_body_field = UCS-2)
      }
```

With (v4.0.23 strict-DCS 不 bypass):
```cpp
      // v4.0.23: 删 is7bit + looks_like_ucs2_be bypass. 信 DCS 严格走 7-bit decode,
      //   不再 sniff bypass (跟 [[dtac-gateway-anomaly]] "不要扩 sniff" 警告一致).
```

- [ ] **Step 3: 删 `src/main.cpp:1925-1932` is_strict_utf8 fallback UCS-2, 改 drop body**

Replace (line 1925-1932):
```cpp
        // 兜底: GSM7 decode 输出必须是合法 UTF-8
        // DCS=0 但实际 UCS-2 (gateway 标错, 泰文 0E23...) 当 7-bit 解会出无效 UTF-8
        // (含 lone continuation / 4+ byte sequence) → fallback UCS-2
        if (bodyN > 0 && !pdu::is_strict_utf8(bodyBuf, bodyN)) {
          ESP_LOGW(TAG, "SMS: 7-bit decode not strict UTF-8, fallback UCS-2");
          bodyN = pdu::decode_body_field(udHex, udFullHexLen, bodyBuf, sizeof(bodyBuf));
          is7bit = false;
        }
```

With (v4.0.23 strict-DCS drop):
```cpp
        // v4.0.23 strict-DCS (case 3): 7-bit decode 输出不 strict UTF-8 → 丢 body
        //   不再 fallback UCS-2 (跟 AOSP 一致). 翔哥 2026-06-22 真机复现 dtac [[dtac-gateway-anomaly]]
        //   raw bytes 高 septet 异常, 7-bit decode 输出非合法 UTF-8 → drop
        if (bodyN > 0 && !pdu::is_strict_utf8(bodyBuf, bodyN)) {
          ESP_LOGW(TAG, "SMS: 7-bit decode not strict UTF-8 (dcs=%u cmt_len=%u udBytes=%u), drop body",
                   msg.dcs, msg.cmt_length, (unsigned)udBytes);
          force_drop = true;
        }
```

- [ ] **Step 4: 在 main.cpp decode 路径加 `force_drop` 标志 + 4 触发条件集中处理**

在 line 1906 `size_t bodyN = 0;` 之前插入 (定义 `force_drop` 标志):

```cpp
      // v4.0.23 strict-DCS: 4 丢 body 触发条件 (集中处理)
      //   case 1: DCS 0x80-0xBF reserved
      //   case 2: DCS 0xFC-0xFF reserved
      //   case 3: 7-bit decode 输出不 strict UTF-8 (sub-byte drift / 错位)
      //   case 4: UDL < UDH size (AOSP bufferLen<0 silent truncation)
      bool force_drop = false;
      // case 1 + 2: DCS reserved (走 is7bit/isUcs2 都 false → is8bitData=true 但 raw 仍乱码)
      if (!is7bit && !isUcs2 && !is8bitData) {
        ESP_LOGW(TAG, "SMS: DCS reserved/unknown (dcs=%u cmt_len=%u), drop body",
                 msg.dcs, msg.cmt_length);
        force_drop = true;
      }
      // case 4: UDL < UDH size (UDH 头污染, bufferLen<0)
      //   计算: UDH size = UDH header (UDHL byte + UDH IE) 总字节数
      //   简化: PDU header UDL byte 已知, FO UDHI=1 时 UDH 占 UDL 头
      //   实际 main.cpp 不知道 UDH size, 跳过这 case, 7-bit decode 后 n==0 + case 3 触发
```

然后修改 line 1906 `size_t bodyN = 0;` 之后的所有 decode, 加 `force_drop` gate:

Replace (line 1906-1950):
```cpp
      size_t bodyN = 0;
      if (is7bit) {
```

With:
```cpp
      size_t bodyN = 0;
      if (force_drop) {
        bodyN = 0;  // strict-DCS drop body
      } else if (is7bit) {
```

(把原来 if (is7bit) { ... } else if (is8bitData) { ... } else { ... } 的开头加 `if (force_drop)` 分支).

- [ ] **Step 5: 镜像 main.cpp 改 fixture helper `fixture_sniffer_decode` (line 1611-1631)**

Replace (line 1611-1631 in `tests/host/test_pdu_codec.cpp`):
```cpp
static size_t fixture_sniffer_decode(const char* body, size_t bodyLen,
                                     bool* outIsUcs2, bool* outIs7bit,
                                     char* utf8, size_t utf8Max) {
  *outIsUcs2 = false; *outIs7bit = false;
  size_t udBytes = 0;
  size_t udOff = 0;
  if (bodyLen >= 4) {
    udOff = pdu::pdu_ud_offset_ex(body, bodyLen, outIsUcs2, outIs7bit, &udBytes);
  }
  size_t udHexOff = 0, udFullHexLen = 0;
  if (udOff > 0) {
    size_t availBytes = (bodyLen - udOff) / 2;
    if (udBytes > availBytes) udBytes = availBytes;
    udHexOff = udOff;
    udFullHexLen = bodyLen - udOff;
  } else {
    udHexOff = 0;
    udBytes = bodyLen / 2;
    udFullHexLen = bodyLen;
  }
  return pdu::decode_body_field(body + udHexOff, udFullHexLen, utf8, utf8Max);
}
```

With (v4.0.23 strict-DCS mirror):
```cpp
static size_t fixture_sniffer_decode(const char* body, size_t bodyLen,
                                     bool* outIsUcs2, bool* outIs7bit,
                                     char* utf8, size_t utf8Max) {
  *outIsUcs2 = false; *outIs7bit = false;
  size_t udBytes = 0;
  size_t udOff = 0;
  if (bodyLen >= 4) {
    udOff = pdu::pdu_ud_offset_ex(body, bodyLen, outIsUcs2, outIs7bit, &udBytes);
  }
  size_t udHexOff = 0, udFullHexLen = 0;
  if (udOff > 0) {
    size_t availBytes = (bodyLen - udOff) / 2;
    if (udBytes > availBytes) udBytes = availBytes;
    udHexOff = udOff;
    udFullHexLen = bodyLen - udOff;
  } else {
    udHexOff = 0;
    udBytes = bodyLen / 2;
    udFullHexLen = bodyLen;
  }
  // v4.0.23 strict-DCS (跟 main.cpp 一致):
  //   pdu_ud_offset_ex 读 TPDU DCS 严格信. DCS 不在 7-bit/UCS-2/8-bit 三类
  //   (outIsUcs2=false && outIs7bit=false) → 丢 body (return 0)
  if (!*outIsUcs2 && !*outIs7bit) {
    return 0;  // DCS reserved/unknown, 跟 AOSP mMessageBody=null 一致
  }
  // 7-bit path: decode 后不 strict UTF-8 → 丢 body (return 0)
  //   host test 镜像: 调 decode_body_field + 简单 UTF-8 strict check
  size_t n = pdu::decode_body_field(body + udHexOff, udFullHexLen, utf8, utf8Max);
  if (*outIs7bit && n > 0) {
    // 检查 UTF-8 strict: 单字节 0x00-0x7F OR 多字节合法 sequence
    for (size_t i = 0; i < n; i++) {
      uint8_t c = (uint8_t)utf8[i];
      if (c < 0x80) continue;  // ASCII, OK
      // 多字节: 检查 continuation + leading byte pattern
      if ((c & 0xE0) == 0xC0) {  // 2-byte
        if (i + 1 >= n || (uint8_t)utf8[i+1] < 0x80 || (uint8_t)utf8[i+1] > 0xBF) return 0;
        i++;
      } else if ((c & 0xF0) == 0xE0) {  // 3-byte
        if (i + 2 >= n) return 0;
        i += 2;
      } else if ((c & 0xF8) == 0xF0) {  // 4-byte
        if (i + 3 >= n) return 0;
        i += 3;
      } else {
        return 0;  // lone continuation / overlong / invalid
      }
    }
  }
  return n;
}
```

- [ ] **Step 6: 增量编译 + 跑 host test, 期望 D/E/F PASS, A FAIL (翔哥 接受 v4.0.24+ 修), B/C 不变**

Run:
```bash
cd /Users/xiang/Claude/Projects/esp32-sms/tests/host && cmake --build build 2>&1 | tail -10 && ./build/test_pdu_codec 2>&1 | tail -5
```

Expected: 输出 `Result: 292 passed, 1 failed` (287 历史 + 3 Fixture A/B/C 改前 pass + 3 D/E/F TDD red 改后 pass - 1 fixture A 改后 fail = 292 pass, 1 fail).

注: `fixture_sniffer_decode` 是 fixture A/B/C/D/E/F 共用的 helper. 改后所有 fixture 跑同一算法. 检查 `B: 单段全绿` + `C: dtac/TRUE 2 段无 regression` 仍 PASS (它们的 PDU 头部干净, 7-bit decode 输出合法 UTF-8).

**fixture A 改后 fail 是翔哥 拍板接受** (2026-06-22 撤回 spec 50bda13 fixture A 改 2 断言决策), 留 v4.0.24+ 修. 跟 [[feedback_working_code_no_touch]] trade 一致: 真机乱码复现要修 (strict-DCS), 顺手破坏 fixture A 接受, 不预先扩 scope.

- [ ] **Step 7: Commit C2 main.cpp + fixture helper**

```bash
cd /Users/xiang/Claude/Projects/esp32-sms && git add src/main.cpp tests/host/test_pdu_codec.cpp && git -c user.email=xiang@anthropic.com -c user.name=xiang commit -m "feat(sms): v4.0.23 strict-DCS, 错位/reserved/无效编码 → 丢 body

翔哥 2026-06-22 真机复现 dtac +66813079348 boot #128 乱码 ([[dtac-gateway-anomaly]] 6/20 同源),
调研 AOSP / smstools3 / python-messaging 业界共识: 严格信 DCS, 错位/reserved = 整条丢.

spec 50bda13 决策: 信 DCS 严格走, 丢 body = 空字符串 (AOSP 严格行为).
3 处删 (sniff 激进策略) + 4 丢 body 触发条件:

删:
- main.cpp:1874-1896 兜底 sniff 3 分支 (looks_like_ucs2_be / cmt_length / concat default 7-bit)
- main.cpp:1897-1905 is7bit + looks_like_ucs2_be bypass
- main.cpp:1925-1932 is_strict_utf8 fallback UCS-2

加:
- force_drop 标志 + case 1/2 (DCS reserved 0x80-0xBF / 0xFC-0xFF)
- case 3 (7-bit decode 不 strict UTF-8 → drop)
- ESP_LOGW 记录丢的原因 (DCS / UDL / udBytes / bodyAvail)

pdu_codec.cpp 0 改 (sniff helper 留供未来重启用, [[dtac-gateway-anomaly]] 警告).

测试 (TDD red → green):
- fixture D/E/F 新加 (DCS=0xFF / 7-bit 不 strict UTF-8 / UDL<UDH size) → body=\"\" PASS
- fixture A 期望调整 (no SCA leak + bodyN=0) → PASS
- fixture B/C 不变 PASS (SCA 部分修 + dtac/TRUE 2 段无 regression)

host test ≥ 293/0 PASS (待 C2 跑后确认实际数, 历史 290 + D/E/F 3 + Fixture A adjust 1)"
```

---

## Task 8: bump FW_VERSION 2 处

**Files:**
- Modify: `src/main.cpp:77` FW_VERSION 宏
- Modify: `src/web/app.h:42` fw-tag

- [ ] **Step 1: bump `src/main.cpp:77` 宏 `"v4.0.22"` → `"v4.0.23"`**

按 [[feedback-bump-fw-version]]: 改代码 bump FW_VERSION 只改 2 处. main.cpp:77 宏跟 app.h:42 fw-tag 同步.

Edit: 把 `src/main.cpp:77` 宏定义 `"v4.0.22"` → `"v4.0.23"`, 注释追加 "v4.0.23: 信 DCS 严格走, 错位/reserved/无效编码 → 丢 body (AOSP 严格行为)".

- [ ] **Step 2: bump `src/web/app.h:42` fw-tag `FW v4.0.22` → `FW v4.0.23`**

Edit: `src/web/app.h:42` `<span class="fw-tag">FW v4.0.22</span>` → `<span class="fw-tag">FW v4.0.23</span>`.

- [ ] **Step 3: 增量编译验证**

Run:
```bash
cd /Users/xiang/Claude/Projects/esp32-sms && pio run 2>&1 | tail -5
```

Expected: 编译 SUCCESS (无业务改动, 只是字符串 bump).

- [ ] **Step 4: Commit bump**

```bash
cd /Users/xiang/Claude/Projects/esp32-sms && git add src/main.cpp src/web/app.h && git -c user.email=xiang@anthropic.com -c user.name=xiang commit -m "chore: bump FW_VERSION v4.0.22 → v4.0.23

按 [[feedback-bump-fw-version]]: 改代码 bump FW_VERSION 只改 2 处.
- src/main.cpp:77 宏 \"v4.0.22\" → \"v4.0.23\" (注释加 v4.0.23 strict-DCS 决策)
- src/web/app.h:42 主页 fw-tag \"FW v4.0.22\" → \"FW v4.0.23\" (唯一可见 fw-tag)

子页 (dashboard/stk/send) fw-tag 是 iframe 死代码, 不 bump.
OTA/Config footer v4.0.13 已删, 0 处需保留 bump."
```

---

## Task 9: 翔哥 code review (审 commit diff)

- [ ] **Step 1: 翔哥 审所有 v4.0.23 commit**

Show:
```bash
cd /Users/xiang/Claude/Projects/esp32-sms && git log --oneline v4.0.22..HEAD
```

翔哥 审:
- TDD red fixture D/E/F (3 commit)
- Fixture A adjust (1 commit)
- C2 main.cpp + fixture helper (1 commit)
- Bump FW_VERSION (1 commit)
- 总 6 commit, src/main.cpp + tests/host/test_pdu_codec.cpp + src/web/app.h 共 3 file

翔哥 拍板后进入 Task 10 (批量烧).

---

## Task 10: 批量烧 + 真机复现

**Files:** (no code change)

- [ ] **Step 1: 烧录**

Run:
```bash
cd /Users/xiang/Claude/Projects/esp32-sms && pio run -t upload 2>&1 | tail -10
```

Expected: `SUCCESS` 烧录完成 (按 [[feedback_code_review_before_flash]] 批量烧, 不迭代).

- [ ] **Step 2: 验证 fw 跟 boot**

Run:
```bash
curl -s http://172.16.1.18/api/status | python3 -m json.tool
```

Expected: `"fw": "v4.0.23"`, `boot` 比 v4.0.22 #128 +1 (即 #129).

- [ ] **Step 3: 验证主页 fw-tag**

Run:
```bash
curl -s http://172.16.1.18/app | grep -o 'FW v[0-9.]*'
```

Expected: `FW v4.0.23`.

- [ ] **Step 4: 真机复现 dtac 乱码 → body="" (按 [[feedback_working_code_no_touch]])**

让翔哥 +66813079348 触发 dtac 推一条真乱码 SMS.

Run:
```bash
curl -s http://172.16.1.18/api/recent | python3 -m json.tool
```

Expected: 最新条目 `phone="dtac"`, `body=""` (空字符串, AOSP 严格行为).

- [ ] **Step 5: 打 tag + push (按 [[feedback_code_review_before_flash]])**

```bash
cd /Users/xiang/Claude/Projects/esp32-sms && git tag -a v4.0.23 -m "v4.0.23: strict-DCS drop body (AOSP 严格行为, dtac 乱码 → body=\"\")" && git push origin v4.0.23 && git push
```

- [ ] **Step 6: 更新 project-state.md + MEMORY.md**

按 [[project-state]] 模板, 加 v4.0.23 行 (commit / boot / 关键改动).

---

## Self-Review

**1. Spec coverage** (spec 50bda13 章节 → plan task):
- Context + 真机复现 + AOSP 调研 → Task 1/2/3 fixture D/E/F 镜像 (✓)
- 决策 (信 DCS 严格 + 空字符串 + 仅 main.cpp) → Task 7 main.cpp decode 路径改 (✓)
- 4 丢 body 触发条件 → Task 7 Step 4 force_drop + Step 5 fixture helper (✓)
- Scope (3 file 改 + 0 pdu_codec) → Task 7/8 (✓)
- 不实现 (out of scope) → Task 7/8 显式不动 (✓)
- Test plan (TDD red fixture D/E/F + Fixture A 期望调整) → Task 1/2/3/5 (✓)
- 烧板验证 → Task 10 (✓)
- bump FW_VERSION 2 处 → Task 8 (✓)
- 风险 (行为变更 / 推送仍触发 / Fixture A 期望调整) → spec 已写, Task 7/10 实施 (✓)

**2. Placeholder scan**: 0 命中 "TBD/TODO/类似/适当". 所有 code block 完整, all commands 完整 with expected output.

**3. Type consistency**:
- `force_drop` (bool, main.cpp) — Task 7 定义 + Step 3 引用 + Step 4 case 1/2 设置, 一致
- `fixture_sniffer_decode` (测试 helper) — Task 7 Step 5 改完整, mirror main.cpp 逻辑, 调用方 (Task 1/2/3/5 fixture D/E/F/A) 不变 (参数/返回类型一致)
- `udBytes`/`udOff`/`udHexOff`/`udFullHexLen` (size_t) — Task 7 Step 5 fixture helper 跟 v4.0.22 一致, 0 类型变化

---

## Execution Handoff

Plan complete and saved to `/Users/xiang/Claude/Projects/esp32-sms/docs/superpowers/plans/2026-06-22-v4-0-23-strict-dcs.md`.

Two execution options:

1. **Subagent-Driven (recommended)** - I dispatch a fresh subagent per task, review between tasks, fast iteration

2. **Inline Execution** - Execute tasks in this session using executing-plans, batch execution with checkpoints

翔哥 选哪个? (按 [[feedback_code_review_before_flash]] + [[feedback_working_code_no_touch]] 我推 Subagent-Driven, 每个 task 审 commit 后再进下个 task)
